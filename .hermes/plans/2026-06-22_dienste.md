# PostfachDienst + KontoDienst — Implementierungsplan

> **Für Hermes:** Nutze den `test-driven-development`-Skill, um diesen Plan TDD-basiert umzusetzen.

**Ziel:** Die Dienst-Klassen in `quelltext/bibliothek/dienst/` implementieren. Sie sind die High-Level-Fassade fürs UI — sie orchestrieren `Datenbank`, `Zwischenspeicher` und (später) `ImapVerbindung`.

**Architektur:** `KontoDienst` verwaltet Konten (CRUD via `Datenbank`). `PostfachDienst` verwaltet ein Postfach — hält den `Zwischenspeicher` aktuell, delegiert Abrufe an `ImapVerbindung` (später).

**Vorbedingung:** Datenbank- und Zwischenspeicher-Pläne abgeschlossen.

---

## Phase 1: KontoDienst

### Aufgabe 1: Test — KontoDienst erstellt und löscht Konten

**Ziel:** `KontoDienst` kann über `Datenbank` Konten anlegen, lesen, löschen.

**Dateien:**
- Erstellen: `pruefungen/tst_konto_dienst.cpp`
- Ändern: `pruefungen/CMakeLists.txt`
- Ändern: `quelltext/bibliothek/dienst/konto_dienst.h`
- Ändern: `quelltext/bibliothek/dienst/konto_dienst.cpp`

**Test-Code (tst_konto_dienst.cpp):**

```cpp
#include <QtCore>
#include <QtTest>
#include "dienst/konto_dienst.h"
#include "speicher/datenbank.h"
#include "kern/konto.h"

using MailAdler::Dienst::KontoDienst;

class TestKontoDienst : public QObject {
    Q_OBJECT

privateSlots:
    void initTestCase() {
        m_db = new Datenbank(this);
        QVERIFY(m_db->oeffne(":memory:"));
        m_dienst = new KontoDienst(m_db, this);
    }

    void sollteKontoAnlegen() {
        auto id = m_dienst->kontoAnlegen(
            "max@test.de", "Max", "imap.test.de", 993,
            "smtp.test.de", 587, "max", "geheim");
        QVERIFY(id > 0);
    }

    void sollteAlleKontenLiefern() {
        m_dienst->kontoAnlegen("zwei@test.de", "", "imap2.de", 993,
                               "smtp2.de", 587, "zwei", "pw");
        auto konten = m_dienst->alleKonten();
        QVERIFY(konten.size() >= 2);
    }

    void sollteKontoLoeschen() {
        auto konten = m_dienst->alleKonten();
        QVERIFY(!konten.isEmpty());
        QVERIFY(m_dienst->kontoLoeschen(konten.first().id));
    }

    void sollteDoppelteEmailAblehnen() {
        m_dienst->kontoAnlegen("unique@test.de", "", "imap", 993,
                               "smtp", 587, "u", "p");
        auto id = m_dienst->kontoAnlegen("unique@test.de", "", "imap2", 993,
                                         "smtp2", 587, "u2", "p2");
        QCOMPARE(id, -1);
    }

private:
    Datenbank *m_db = nullptr;
    KontoDienst *m_dienst = nullptr;
};

QTEST_MAIN(TestKontoDienst)
#include "tst_konto_dienst.moc"
```

**Vor dem Bauen:** `pruefungen/CMakeLists.txt`:
```cmake
mailadler_pruefung_hinzufuegen(tst_konto_dienst)
```

### Aufgabe 2: Implementierung — KontoDienst

**Header (konto_dienst.h):**

```cpp
#pragma once
#include "../kern/qt_alias.h"
#include <QtCore/QObject>
#include <QtCore/QVector>
#include "../kern/konto.h"

namespace MailAdler {

namespace Speicher { class Datenbank; }

namespace Dienst {

class KontoDienst : public QObject {
    Q_OBJECT
public:
    explicit KontoDienst(Speicher::Datenbank *db, QObject *eltern = nullptr);

    qint64 kontoAnlegen(const QString &email, const QString &name,
                        const QString &imapServer, quint16 imapPort,
                        const QString &smtpServer, quint16 smtpPort,
                        const QString &benutzer, const QString &passwort);

    QVector<Kern::Konto> alleKonten() const;
    bool kontoLoeschen(qint64 id);

signale:
    void kontoHinzugefuegt(qint64 id);
    void kontoGeloescht(qint64 id);
    void fehlerAufgetreten(const QString &meldung);

private:
    Speicher::Datenbank *m_db = nullptr;
};

}} // namespace
```

**Implementierung (konto_dienst.cpp):**

```cpp
#include "konto_dienst.h"
#include "../speicher/datenbank.h"

namespace MailAdler { namespace Dienst {

KontoDienst::KontoDienst(Speicher::Datenbank *db, QObject *eltern)
    : QObject(eltern), m_db(db) {}

qint64 KontoDienst::kontoAnlegen(const QString &email, const QString &name,
                                  const QString &imapServer, quint16 imapPort,
                                  const QString &smtpServer, quint16 smtpPort,
                                  const QString &benutzer, const QString &passwort) {
    Kern::Konto k;
    k.email       = email;
    k.name        = name;
    k.imapServer  = imapServer;
    k.imapPort    = imapPort;
    k.smtpServer  = smtpServer;
    k.smtpPort    = smtpPort;
    k.benutzer    = benutzer;
    k.passwort    = passwort;
    k.istAktiv    = true;

    qint64 id = m_db->kontoSpeichern(k);
    if (id > 0) emit kontoHinzugefuegt(id);
    else emit fehlerAufgetreten("Konto konnte nicht angelegt werden");
    return id;
}

QVector<Kern::Konto> KontoDienst::alleKonten() const {
    return m_db->alleKonten();
}

bool KontoDienst::kontoLoeschen(qint64 id) {
    bool ok = m_db->kontoLoeschen(id);
    if (ok) emit kontoGeloescht(id);
    return ok;
}

}} // namespace
```

**Bauen + Test:**

```bash
cmake --build bau --parallel $(nproc)
ctest --test-dir bau --output-on-failure -R konto_dienst -V
```

Erwartet: 3–4 Tests PASS.

**Commit:**

```bash
git add pruefungen/tst_konto_dienst.cpp pruefungen/CMakeLists.txt \
        quelltext/bibliothek/dienst/konto_dienst.h \
        quelltext/bibliothek/dienst/konto_dienst.cpp
git commit -m "dienst: KontoDienst implementiert + Tests"
```

---

## Phase 2: PostfachDienst

### Aufgabe 3: Test — PostfachDienst

**Ziel:** `PostfachDienst` kombiniert `Zwischenspeicher` + `Datenbank` für ein Postfach.

**Dateien:**
- Erstellen: `pruefungen/tst_postfach_dienst.cpp`
- Ändern: `pruefungen/CMakeLists.txt`
- Ändern: `quelltext/bibliothek/dienst/postfach_dienst.h`
- Ändern: `quelltext/bibliothek/dienst/postfach_dienst.cpp`

**Test-Code (tst_postfach_dienst.cpp):**

```cpp
#include <QtCore>
#include <QtTest>
#include "dienst/postfach_dienst.h"
#include "speicher/datenbank.h"
#include "speicher/zwischenspeicher.h"

using MailAdler::Dienst::PostfachDienst;
using MailAdler::Speicher::Datenbank;
using MailAdler::Speicher::Zwischenspeicher;

class TestPostfachDienst : public QObject {
    Q_OBJECT

privateSlots:
    void initTestCase() {
        m_cache = new Zwischenspeicher(this);
        m_dienst = new PostfachDienst(m_cache, this);
    }

    void sollteLeerStarten() {
        QVERIFY(m_dienst->nachrichten().isEmpty());
        QCOMPARE(m_dienst->anzahl(), 0);
    }

    void sollteNachrichtenSetzen() {
        Kern::Nachricht n;
        n.id = 1; n.betreff = "Test";
        m_dienst->setzeNachrichten({n});

        QCOMPARE(m_dienst->anzahl(), 1);
        QCOMPARE(m_dienst->nachrichten()[0].betreff, "Test");
    }

    void sollteSignaleSenden() {
        QSignalSpy spion(m_dienst, &PostfachDienst::nachrichtenGeaendert);

        Kern::Nachricht n;
        m_dienst->setzeNachrichten({n});

        QCOMPARE(spion.count(), 1);
    }

private:
    Zwischenspeicher *m_cache = nullptr;
    PostfachDienst *m_dienst = nullptr;
};

QTEST_MAIN(TestPostfachDienst)
#include "tst_postfach_dienst.moc"
```

**CMakeLists.txt:**
```cmake
mailadler_pruefung_hinzufuegen(tst_postfach_dienst)
```

### Aufgabe 4: Implementierung — PostfachDienst

**Header (postfach_dienst.h):**

```cpp
#pragma once
#include "../kern/qt_alias.h"
#include <QtCore/QObject>
#include <QtCore/QVector>
#include "../kern/nachricht.h"

namespace MailAdler {

namespace Speicher { class Zwischenspeicher; }

namespace Dienst {

class PostfachDienst : public QObject {
    Q_OBJECT
public:
    explicit PostfachDienst(Speicher::Zwischenspeicher *cache,
                            QObject *eltern = nullptr);

    int anzahl() const;
    QVector<Kern::Nachricht> nachrichten() const;
    void setzeNachrichten(const QVector<Kern::Nachricht> &nachrichten);

signale:
    void nachrichtenGeaendert();

private:
    Speicher::Zwischenspeicher *m_cache = nullptr;
};

}} // namespace
```

**Implementierung (postfach_dienst.cpp):**

```cpp
#include "postfach_dienst.h"
#include "../speicher/zwischenspeicher.h"

namespace MailAdler { namespace Dienst {

PostfachDienst::PostfachDienst(Speicher::Zwischenspeicher *cache, QObject *eltern)
    : QObject(eltern), m_cache(cache) {}

int PostfachDienst::anzahl() const {
    return m_cache->alle().size();
}

QVector<Kern::Nachricht> PostfachDienst::nachrichten() const {
    return m_cache->alle();
}

void PostfachDienst::setzeNachrichten(const QVector<Kern::Nachricht> &n) {
    m_cache->setze(n);
    emit nachrichtenGeaendert();
}

}} // namespace
```

**Bauen + Test:**

```bash
cmake --build bau --parallel $(nproc)
ctest --test-dir bau --output-on-failure -R postfach_dienst -V
```

Erwartet: 3 Tests PASS.

**Commit:**

```bash
git add pruefungen/tst_postfach_dienst.cpp pruefungen/CMakeLists.txt \
        quelltext/bibliothek/dienst/postfach_dienst.h \
        quelltext/bibliothek/dienst/postfach_dienst.cpp
git commit -m "dienst: PostfachDienst implementiert + Tests"
```

---

## Zusammenfassung

| Phase | Modul | Tests | Abhängigkeiten |
|---|---|---|---|
| 1 | `KontoDienst` | 4 | `Datenbank` ✅ |
| 2 | `PostfachDienst` | 3 | `Zwischenspeicher` ✅ |

**Danach:** Die Dienst-Schicht ist fertig. Das UI kann Konten verwalten und Nachrichtenlisten anzeigen. Der nächste Plan ist `ImapVerbindung` — die eigentliche Netzwerk-Arbeit.
